openapi: 3.0.0
info:
  title: Access Gate API
  version: 1.0.0
description: |-
  OpenAPI specification for the Access Gate mock API.
servers:
  - url: http://localhost:4010
paths:
  /health:
    get:
      summary: Health check endpoint
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok